# Production env — loaded automatically by Expo for production builds
# (eas build / expo export). Deploy the backend first, then put the real HTTPS
# URL here. Must be HTTPS (App Store / Play Store block cleartext by default).
EXPO_PUBLIC_API_URL=https://your-deployed-api.example.com
